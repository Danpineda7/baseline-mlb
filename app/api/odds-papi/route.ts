import { syncOddsPapiDate } from "@/lib/oddspapi";
import { requireAdmin } from "@/lib/admin-auth";

export async function POST(request:Request){const admin=await requireAdmin(request);if(!admin.ok)return admin.response;const {env}=await import("cloudflare:workers");const runtime=env as unknown as Record<string,unknown>,key=runtime.ODDS_PAPI_KEY;if(typeof key!=="string"||!key)return Response.json({error:"The free OddsPapi key is not configured."},{status:503});try{const body=await request.json() as {date?:string;limit?:number;games?:Array<{id:number;awayTeam:string;homeTeam:string}>};if(!body.date?.match(/^\d{4}-\d{2}-\d{2}$/))return Response.json({error:"A valid slate date is required."},{status:400});const games=(body.games??[]).filter(game=>Number.isInteger(game.id)&&game.awayTeam&&game.homeTeam);const result=await syncOddsPapiDate((env as unknown as {DB:Parameters<typeof syncOddsPapiDate>[0]}).DB,key,body.date,games,body.limit);return Response.json(result);}catch(error){return Response.json({error:error instanceof Error?error.message:"Odds import failed."},{status:502});}}

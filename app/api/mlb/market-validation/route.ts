import { marketValidation } from "@/lib/market-validation";
type Forecast={game_id:number;market:string;selection_key:string;line:number|null;probability:number;outcome:number;starts_at:string};
type Observation={game_id:number;market:string;selection:string;line:number|null;american_odds:number;observed_at:string;sportsbook:string;metadata_json:string};
export async function GET(){try{const {env}=await import("cloudflare:workers"),forecastQuery=await env.DB.prepare("SELECT game_id,market,selection_key,line,probability,outcome,starts_at FROM forecast_snapshots WHERE status IN ('WON','LOST') ORDER BY game_date DESC LIMIT 5000").all<Forecast>(),oddsQuery=await env.DB.prepare("SELECT o.game_id,o.market,o.selection,o.line,o.american_odds,o.observed_at,o.sportsbook,o.metadata_json FROM market_odds_observations o WHERE o.game_id IS NOT NULL AND EXISTS (SELECT 1 FROM forecast_snapshots f WHERE f.game_id=o.game_id AND f.status IN ('WON','LOST')) ORDER BY o.observed_at DESC LIMIT 50000").all<Observation>();return Response.json({retrievedAt:new Date().toISOString(),...marketValidation(forecastQuery.results??[],oddsQuery.results??[])},{headers:{"cache-control":"no-store"}});}catch(error){return Response.json({error:"Market validation is initializing.",detail:error instanceof Error?error.message:"Unknown database error"},{status:503});}}
